<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Admin Login - AI Security Command Center</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
        rel="stylesheet" />
</head>

<body class="bg-cover bg-center bg-fixed min-h-screen flex items-center justify-center"
    style="background-image: url('/img/veths.jpg'); font-family: 'Inter', sans-serif; color: #e2e8f0;">
    <div class="inset-0 bg-gray-900 bg-opacity-70 z-10 min-h-screen fixed"></div>

    <div class=" p-8  z-20 rounded-2xl w-full max-w-sm">
        <h1 class="text-2xl font-semibold text-center text-white mb-6">Admin Login</h1>
        <form method="POST" action="/api/admin/login" class="flex flex-col space-y-6">
            <div class="text-center text-red-500 bg-gray-50 rounded bg-opacity-80 text-sm py-1 font-semibold mt-4">
                <p>Enter your Credentials to access the system.</p>
            </div>
            <input type="email" name="email" placeholder="Email" required autofocus
                class="w-full px-4 py-3 rounded-md text-gray-900 text-lg focus:outline-none focus:ring-2 focus:ring-green-500" />
            <input type="password" name="password" placeholder="Password" required
                class="w-full px-4 py-3 rounded-md text-gray-900 text-lg focus:outline-none focus:ring-2 focus:ring-green-500" />


            <button type="submit"
                class="w-full bg-green-600 hover:bg-green-700 text-white font-semibold py-3 rounded-md shadow-md transition duration-300 ease-in-out transform hover:scale-105">
                Submit
            </button>
            <div class="text-center text-sm text-gray-400 mt-4">
                <p>Forgotten password?<a href="/api/admin/resetpassword" class="text-green-500 hover:underline"> reset password here</a></p>
            </div>
        </form>
    </div>
    <script src="/js/adminlogin.js"></script>
    <div id="toast-container" class="fixed top-6 right-6 z-50 space-y-2"></div>
</body>

</html>