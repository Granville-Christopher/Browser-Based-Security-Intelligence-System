<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
        <%= title %> - AI Security Command Center
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
        rel="stylesheet" />
    <meta name="csrf-token" content="<%= csrfToken %>">
</head>

<body class="bg-gray-900 text-gray-100 font-sans min-h-screen flex flex-col overflow-x-hidden">

    <header class="bg-gray-800 p-4 space-x-4 shadow-md flex items-center fixed top-0 left-0 right-0 z-50 justify-between">
        <h1 class="md:text-2xl text-sm font-bold text-white">
            <%= title %>
        </h1>
        <nav class="space-x-4 text-right">
            <a href="/api/admin/admin" class="text-green-400 text-sm md:text-base hover:text-green-600 font-semibold">Dashboard</a>
            <a href="/logout" class="text-green-400 text-sm md:text-base hover:text-green-600 font-semibold">Logout</a>
        </nav>
    </header>