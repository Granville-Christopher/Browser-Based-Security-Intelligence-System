<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Admin Reset Password - AI Security Command Center</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
        rel="stylesheet" />
</head>

<body class="bg-cover bg-center bg-fixed min-h-screen flex items-center justify-center"
    style="background-image: url('/img/veths.jpg'); font-family: 'Inter', sans-serif; color: #e2e8f0;">
    <div class="bg-gray-900 bg-opacity-90 p-8 rounded-lg shadow-lg w-full max-w-md space-y-8">
        <!-- Toast Container -->
        <div id="toast-container" class="fixed top-5 right-5 space-y-2 z-50"></div>

        <!-- Form 1: Get OTP -->
        <form id="get-otp-form" class="flex items-center space-x-4">
            <input type="email" name="email" placeholder="Enter your email" required
                class="flex-grow px-4 py-3 rounded-md text-gray-900 text-lg focus:outline-none focus:ring-2 focus:ring-green-500" />
            <button type="submit"
                class="bg-green-600 hover:bg-green-700 text-white font-semibold py-3 px-6 rounded-md shadow-md transition duration-300 ease-in-out transform hover:scale-105">
                Get OTP
            </button>
        </form>

        <!-- Form 2: Reset Password -->
        <form id="reset-password-form" class="flex flex-col space-y-6">
            <input type="email" name="email" placeholder="Email" required
                class="px-4 py-3 rounded-md text-gray-900 text-lg focus:outline-none focus:ring-2 focus:ring-green-500" />

            <input type="text" name="otp" placeholder="Enter OTP" required
                class="px-4 py-3 rounded-md text-gray-900 text-lg focus:outline-none focus:ring-2 focus:ring-green-500" />

            <div class="relative">
                <input type="password" name="new_password" placeholder="New Password" required id="new-password"
                    class="w-full px-4 py-3 rounded-md text-gray-900 text-lg focus:outline-none focus:ring-2 focus:ring-green-500" />
                <button type="button" id="toggle-new-password" onclick="togglePasswordVisibility('new-password')"
                    class="absolute right-3 top-3 text-xl text-gray-600 hover:text-gray-900 focus:outline-none"
                    title="Toggle Password">
                    👁️
                </button>
            </div>

            <div class="relative">
                <input type="password" name="confirm_new_password" placeholder="Confirm New Password" required
                    id="confirm-new-password"
                    class="w-full px-4 py-3 rounded-md text-gray-900 text-lg focus:outline-none focus:ring-2 focus:ring-green-500" />
                <button type="button" id="toggle-confirm-new-password" onclick="togglePasswordVisibility('confirm-new-password')"
                    class="absolute right-3 top-3 text-xl text-gray-600 hover:text-gray-900 focus:outline-none"
                    title="Toggle Password">
                    👁️
                </button>
            </div>

            <button type="submit"
                class="bg-green-600 hover:bg-green-700 text-white font-semibold py-3 rounded-md shadow-md transition duration-300 ease-in-out transform hover:scale-105">
                Reset Password
            </button>
        </form>

        <div class="text-center text-gray-400 mt-4">
            <p>
                Remembered your password?
                <a href="/api/admin/login" class="text-green-500 hover:text-green-700 font-semibold">Login</a>
            </p>
        </div>
    </div>
    <script defer src="/js/resetpassword.js"></script>
</body>

</html>